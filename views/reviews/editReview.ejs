<!-- views/reviews/editReview.ejs
<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit Review</title>
</head>
<body>

    <h1>Edit Your Review</h1>

    <a href="/song/songDetail?songID=<%= song.song_id %>">Back to Song Detail</a>

    <hr>

    <% if (song) { %>
        <h2><%= song.songname %></h2>
        <p><strong>Artist:</strong> <%= song.artist %></p>
        <p><strong>Category:</strong> <%= song.category %></p>
        <p><strong>Description:</strong> <%= song.description %></p>
    <% } %>

    <hr>

    <% if (error && error !== "") { %>
        <p><%- error %></p>
    <% } %>

    <form action="/review/edit-review" method="post">
        <input type="hidden" name="song_id" value="<%= song.song_id %>">

        <label for="rating">Rating:</label>
        <select name="rating" id="rating">
            <option value="1" <%= userReview && userReview.rating == 1 ? "selected" : "" %>>1</option>
            <option value="2" <%= userReview && userReview.rating == 2 ? "selected" : "" %>>2</option>
            <option value="3" <%= userReview && userReview.rating == 3 ? "selected" : "" %>>3</option>
            <option value="4" <%= userReview && userReview.rating == 4 ? "selected" : "" %>>4</option>
            <option value="5" <%= userReview && userReview.rating == 5 ? "selected" : "" %>>5</option>
        </select>

        <br><br>

        <label for="reviewMessage">Review Message:</label><br>
        <textarea name="reviewMessage" id="reviewMessage" rows="5" cols="50"><%= userReview ? userReview.reviewMessage : "" %></textarea>

        <br><br>

        <button type="submit">Update Review</button>
    </form>

</body>
</html> -->