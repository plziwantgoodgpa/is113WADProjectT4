<!-- views/reviews/deleteReview.ejs
<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Delete Review</title>
</head>
<body>

    <h1>Delete Your Review</h1>

    <a href="/song/songDetail?songID=<%= song.song_id %>">Back to Song Detail</a>

    <hr>

    <% if (song) { %>
        <h2><%= song.songname %></h2>
        <p><strong>Artist:</strong> <%= song.artist %></p>
        <p><strong>Category:</strong> <%= song.category %></p>
        <p><strong>Description:</strong> <%= song.description %></p>
    <% } %>

    <hr>

    <% if (userReview) { %>
        <p><strong>Your Rating:</strong> <%= userReview.rating %></p>
        <p><strong>Your Review:</strong> <%= userReview.reviewMessage %></p>
        <p><strong>Date:</strong> <%= userReview.review_date_time %></p>

        <p>Are you sure you want to delete this review?</p>

        <form action="/review/delete-review" method="post">
            <input type="hidden" name="song_id" value="<%= song.song_id %>">
            <button type="submit">Yes, Delete Review</button>
        </form>
    <% } else { %>
        <p>No review found.</p>
    <% } %>

</body>
</html> -->